# 软件测试平台 · 简历链接（直接复制粘贴即可 · 全部已替换为真实账号）

> 生成时间：2026-08-12
> 真实 GitHub 账号：zy-123913
> 真实仓库名：software-testing-demo

---

## 🟢 第一部分：最推荐写简历的 2 条公网链接（✅ 全部真实可用，面试官任何地点都能点开）

### 🎯 1. 在线演示页（GitHub Pages，面试官点链接直接操作！）
```
https://zy-123913.github.io/software-testing-demo/
```
> 如果点开会 404，按下面「开启 GitHub Pages 教程」操作一次，1 分钟就生效。  
> 离线本地 IndexedDB Mock 版本，完整 9 个 Tab：用户管理 / 商品管理 / 订单交易 / 接口测试 / 运行测试 / 性能测试 等全部可操作，注册、下单、总成交额实时更新全部演示无压力。

### 📂 2. 源代码仓库（面试官看完整代码 + 测试用例 + 分层架构）
```
https://github.com/zy-123913/software-testing-demo
```
> 源码包含三层架构（Entity/DAO/Service）、152+ 条完整测试：
> 单元测试（JUnit5+Mockito）/ API 自动化（RestAssured）/ UI 自动化（Playwright+POM）/ 性能测试（JMeter）/ JaCoCo 覆盖率 / Allure 报告 / Dockerfile / render.yaml 部署配置。

---

## 🟡 第二部分：100% 本地可用链接（现场面试备用，不用联网）

✅ **不需要任何部署 · 复制到浏览器地址栏立即打开**

| 场景 | 链接（直接整行复制） | 使用说明 |
|---|---|---|
| **🎯 现场面试演示（真后端全流程）** | `http://localhost:8099/` | 面试前 1 分钟，双击项目根目录 `start.bat` 启动 Java 后端 → 点此链接完整演示：注册用户 → 充值 → 下单购买 → 总成交额实时 ¥6999 → 发货 → 确认收货，全流程真实跑通（非Mock） |
| **📴 离线演示（不用启后端，面试前怕出问题用这个）** | `file:///E:/xiangmu/test1/docs/index.html` | 任何时候双击 `docs/index.html` 都能直接开，和 GitHub Pages 效果一致 |
| **📂 源码项目入口（面试现场翻代码用）** | `file:///E:/xiangmu/test1/` | 直接打开项目文件夹 |

---

## 🟡 如果上面 GitHub Pages 还是 404，按下面 3 步开启（只需要点几下）

你的代码（`main` 分支 + `/docs` 文件夹）已经 push 到仓库了，只差最后一步开启 Pages：

1. 打开仓库 → 顶部 **Settings** → 左侧菜单拉到最下面找到 **Pages**
2. **Build and deployment** → Source 选 `Deploy from a branch`
3. **Branch** 选 `main` → 右边下拉选 **`/docs`** → 点 **Save**
4. 等 **1-2 分钟**（右上角有橙色小点表示正在编译），再访问上面的 `https://zy-123913.github.io/software-testing-demo/` 就彻底 OK 了 ✨

---

## 🔵 第三部分：加分项（可选）——真 Java 后端公网部署

> GitHub Pages 只是纯前端离线 Mock。如果你想让面试官点链接进去**真注册真下单、数据全局共享**（不是本地Mock），部署 Render 真后端 Docker（免费）：

项目根目录已经有 `Dockerfile` 和 `render.yaml`，3 步部署：
1. 打开 [render.com](https://render.com) → 用 GitHub 账号（zy-123913）一键登录
2. 右上角 **New** → **Web Service** → 选 `software-testing-demo` 仓库
3. Runtime 自动识别为 **Docker** → Instance Type 选 **Free**（免费套餐） → 点 **Create Web Service**
4. 等 3-5 分钟，部署成功后得到一个真后端公网链接（例如 `https://software-testing-demo.onrender.com`），把第一部分的第 1 条替换成这个，演示效果直接拉满（面试官能注册账号，你下一个单对面也能看到）。

---

## 📝 简历项目描述（直接复制，结构完整不用改）

```
项目名称：软件测试一体化平台 — JUnit 5 + RestAssured + Playwright + JMeter + 三层架构电商系统

项目链接：
  • 在线演示（GitHub Pages 公网，面试官直接操作）：https://zy-123913.github.io/software-testing-demo/
  • 源码仓库：                                          https://github.com/zy-123913/software-testing-demo
  • 现场演示（面试前双击 start.bat）：                  http://localhost:8099/

项目描述：
  基于 Java + Maven 构建的前后端一体化软件测试平台，覆盖单元测试、API 自动化测试、UI 自动化测试、
  性能测试 4 大软件测试类型，同时实现了一套三层架构（Entity / DAO / Service）的电商业务系统
  （用户管理、商品管理、订单交易），用于支撑真实测试场景。

技术栈 & 项目亮点：
  1) 单元测试：JUnit 5 + Mockito，覆盖计算器 / 字符串工具 / 用户校验工具类，边界用例齐全
  2) API 自动化：RestAssured + Allure 可视化报告，覆盖用户 / 商品 / 订单三层接口的 CRUD 业务
  3) UI 自动化：Playwright + POM（页面对象模型），覆盖计算器 / 测试运行 / 电商业务页面交互
  4) 性能测试：JMeter JMX 脚本 + 参数化，支持多线程高并发压测
  5) 质量度量：JaCoCo 代码覆盖率统计 + Allure 报告生成
  6) 业务场景：电商用户 / 商品 / 订单全生命周期，注册、充值、下单、发货、收货、退款状态流转
  7) 可视化界面：9 Tab 完整前端，总成交额实时统计（PAID / SHIPPED / COMPLETED 三状态口径），
     所有操作均可通过页面完成，便于面试现场演示
  8) 部署交付：GitHub Pages 在线可操作演示页 + Dockerfile + render.yaml 一键部署真后端
```
